export interface ProductValue {
    id:number,
    catogary:string,
    quantity:string,
    price:string,
    stock:string,
  }


export interface data {
  id:number,
  catogary:string,
  quantity:number,
  price:number,
  stock:string
}