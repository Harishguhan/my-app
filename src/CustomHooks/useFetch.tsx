import { useState } from 'react';
const useFetch = () => {
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const data = useState<any>('pharmacy inventory system');
  return data;
};
export default useFetch;
